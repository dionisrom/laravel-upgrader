{
    "name": "laravel-upgrader/laravel-upgrader",
    "description": "Automated Laravel Enterprise Upgrader — upgrades Laravel 8→13 and PHP 8.0→8.5",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": "^8.2",
        "ramsey/uuid": "^4.0",
        "react/event-loop": "^1.3",
        "react/http": "^1.9",
        "react/socket": "^1.14",
        "symfony/console": "^6.4 || ^7.0",
        "symfony/process": "^6.4 || ^7.0",
        "twig/twig": "^3.4"
    },
    "require-dev": {
        "driftingly/rector-laravel": "^2.0",
        "phpstan/phpstan": "^2.0",
        "phpunit/phpunit": "^11.0",
        "rector/rector": "^2.3",
        "squizlabs/php_codesniffer": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/",
            "AppContainer\\": "src-container/"
        },
        "exclude-from-classmap": [
            "tests/Fixtures/",
            "tests/Unit/Rector/Rules/"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "bin": ["bin/upgrader"],
    "scripts": {
        "test": "phpunit --configuration phpunit.xml.dist --testsuite unit --no-coverage",
        "test:integration": "phpunit --configuration phpunit.xml.dist --testsuite integration --no-coverage",
        "test:e2e": "phpunit --configuration phpunit.xml.dist --testsuite e2e --no-coverage",
        "phpstan": "phpstan analyse --no-progress --memory-limit=512M",
        "cs-check": "phpcs --standard=PSR12 src src-container"
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "minimum-stability": "stable"
}
