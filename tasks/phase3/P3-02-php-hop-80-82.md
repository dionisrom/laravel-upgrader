# P3-02: PHP 8.0→8.1 & 8.1→8.2 Hop Containers

**Phase:** 3  
**Priority:** Must Have  
**Estimated Effort:** 8-10 days  
**Dependencies:** P3-01 (2D HopPlanner), Phase 1 Docker hop pattern  
**Blocks:** P3-05 (Extension Compatibility Checker), P3-08 (Combined Mode Testing)  

---

## Agent Persona

**Role:** Docker/DevOps Engineer  
**Agent File:** `agents/docker-devops-engineer.agent.md`  
**Domain Knowledge Required:**
- PHP 8.0→8.1 breaking changes: enums, readonly properties, `never` return type, intersection types, fibers
- PHP 8.1→8.2 breaking changes: readonly classes, DNF types, deprecated `${var}` strings, `utf8_encode()` removal
- Rector `LevelSetList` configuration (primary transformation engine for PHP hops)
- PHP hop verification pipeline: syntax check + PHPStan baseline delta (no Artisan)
- TRD §21: PHP Hop Container Architecture

---

## Objective

Build the first two PHP hop Docker images (`upgrader:php-8.0-to-8.1` and `upgrader:php-8.1-to-8.2`). PHP hops are simpler than Laravel hops because Rector's `LevelSetList` provides strong built-in coverage — custom rules are rarely needed.

---

## Context from PRD & TRD

### Docker Images (PRD §11.1)

| Image | PHP Base | Key Breaking Changes | Rector Set |
|---|---|---|---|
| `upgrader:php-8.0-to-8.1` | PHP 8.1 | Enums, readonly, `never`, intersection types, fibers | `LevelSetList::UP_TO_PHP_81` |
| `upgrader:php-8.1-to-8.2` | PHP 8.2 | Readonly classes, DNF types, `${var}` deprecated, `utf8_encode()` removal | `LevelSetList::UP_TO_PHP_82` |

### Rector Config Pattern (PRD §11.4)

```php
// rector-configs/rector.php80-to-php81.php
use Rector\Config\RectorConfig;
use Rector\Set\ValueObject\LevelSetList;

return RectorConfig::configure()
    ->withPaths(['/workspace'])
    ->withSets([LevelSetList::UP_TO_PHP_81])
    ->withPhpSets(php81: true);
```

### PHP Hop Verification Pipeline (PRD §11.3)

```
├── php -l (syntax check every file)
├── PHPStan baseline delta (parallel, cached)
├── composer validate
├── composer install
└── SilentChangeScanner (Phase 3 later — stub interface for now)
```

### Key Differences from Laravel Hops

- Uses Rector `LevelSetList` (built-in) — no custom Rector rules needed
- Verification is lighter: no Artisan boot, no config:cache, no route:list
- `--network=none` during transform (same as Laravel hops)
- `TransformCheckpoint` used (same resume model)

---

## Files to Create

| File | Location | Purpose |
|---|---|---|
| `Dockerfile` | `docker/php-8.0-to-8.1/` | PHP 8.1 base image |
| `entrypoint.sh` | `docker/php-8.0-to-8.1/` | Pipeline entrypoint |
| `php-breaking-changes.json` | `docker/php-8.0-to-8.1/docs/` | PHP 8.0→8.1 changes |
| `rector.php80-to-php81.php` | `rector-configs/` | Rector config |
| `Dockerfile` | `docker/php-8.1-to-8.2/` | PHP 8.2 base image |
| `entrypoint.sh` | `docker/php-8.1-to-8.2/` | Pipeline entrypoint |
| `php-breaking-changes.json` | `docker/php-8.1-to-8.2/docs/` | PHP 8.1→8.2 changes |
| `rector.php81-to-php82.php` | `rector-configs/` | Rector config |
| `PhpHopVerifier.php` | `src/Verification/` | PHP-specific verification pipeline |
| Test fixtures | `tests/Unit/PhpHop/` | PHP hop tests |

---

## Acceptance Criteria

- [ ] Both Docker images build for amd64/arm64
- [ ] PHP 8.0→8.1 hop transforms using `LevelSetList::UP_TO_PHP_81`
- [ ] PHP 8.1→8.2 hop transforms using `LevelSetList::UP_TO_PHP_82`
- [ ] `php-breaking-changes.json` bundled in each image
- [ ] PHP hop verification: `php -l` + PHPStan baseline delta + `composer validate`
- [ ] `--network=none` enforced during transform
- [ ] `TransformCheckpoint` works for PHP hops (resume capability)
- [ ] Fixture PHP projects upgrade correctly through both hops
