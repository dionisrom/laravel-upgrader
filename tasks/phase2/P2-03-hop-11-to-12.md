# P2-03: L11→L12 Hop Container & Rules

**Phase:** 2  
**Priority:** Must Have  
**Estimated Effort:** 6-8 days  
**Dependencies:** Phase 1 Docker pattern, P2-01 (PackageRuleActivator)  
**Blocks:** P2-05 (Multi-Hop Orchestration)  

---

## Agent Persona

**Role:** Rector/AST Transformation Engineer  
**Agent File:** `agents/rector-ast-engineer.agent.md`  
**Domain Knowledge Required:**
- Laravel 11→12 breaking changes (route model binding, `once()` helper, Eloquent/collection changes)
- Rector rule authoring for API signature changes
- Docker image build patterns (established in Phase 1/P2-01)

---

## Objective

Build `upgrader:hop-11-to-12` Docker image, curate L11→L12 breaking changes, implement custom Rector rules for changes not covered by `rector-laravel`, focusing on route model binding changes and the `once()` helper introduction.

---

## Context from PRD & TRD

### Key L11→L12 Breaking Changes (PRD §3.2)

- Route model binding changes
- `once()` helper introduced
- Various Eloquent and collection changes

### Docker Image (PRD §3.1, TRD-P2HOP-001)

| Image | PHP Base | Key Custom Modules |
|---|---|---|
| `upgrader:hop-11-to-12` | `php:8.2-cli-alpine` | `RouteBindingAuditor`, `OnceHelperIntroducer` |

---

## Files to Create

| File | Location | Purpose |
|---|---|---|
| `Dockerfile` | `docker/hop-11-to-12/` | PHP 8.2 base image |
| `entrypoint.sh` | `docker/hop-11-to-12/` | Pipeline entrypoint |
| `breaking-changes.json` | `docker/hop-11-to-12/docs/` | L11→L12 changes |
| `RouteBindingAuditor.php` | `src-container/Rector/Rules/L11ToL12/` | Route binding changes |
| `OnceHelperIntroducer.php` | `src-container/Rector/Rules/L11ToL12/` | once() helper |
| `rector.l11-to-l12.php` | `rector-configs/` | Rector config |
| Test fixtures | `tests/Unit/Rector/Rules/L11ToL12/` | Rule tests |

---

## Acceptance Criteria

- [ ] Docker image builds for both platforms
- [ ] Complete `breaking-changes.json` for L11→L12
- [ ] Route model binding changes handled or flagged
- [ ] `once()` helper introduced where applicable
- [ ] All custom rules have fixture tests
- [ ] L11 fixture app upgrades to L12 with verification pass
