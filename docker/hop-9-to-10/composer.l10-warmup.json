{
    "name": "upgrader/l10-warmup",
    "description": "Composer cache warmup for Laravel 10 target packages. Used at Docker build time only — populates the Composer HTTP cache so DependencyUpgrader can run composer install offline (--network=none) at container runtime.",
    "type": "project",
    "require": {
        "php": "^8.1",
        "laravel/framework": "^10.0",
        "guzzlehttp/guzzle": "^7.0",
        "laravel/horizon": "^5.15",
        "laravel/passport": "^11.8",
        "laravel/sanctum": "^3.3",
        "livewire/livewire": "^3.0",
        "spatie/laravel-activitylog": "^4.7",
        "spatie/laravel-permission": "^6.0"
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "sort-packages": true
    }
}
